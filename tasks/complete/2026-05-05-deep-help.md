---
status: complete
size: medium
branch: deep-help
pr: 197
---

# Render deep command help

## Status Summary

Implementation is complete. `deepHelp(program)` is exported as a standalone
helper, renders visible Commander command nodes depth-first with full
`helpInformation()` blocks, and has README coverage plus focused tests. No known
implementation pieces are missing; PR review remains.

## Summary Ask

Design and implement a way to render help for every command in a generated CLI
tree, not just the currently selected command. This should help humans and
agents inspect the complete command surface for nested routers.

Selected surface:

```ts
import {createCli, deepHelp} from 'trpc-cli'

const cli = createCli({router})
console.log(deepHelp(cli.buildProgram()))
```

Users can expose the returned string however they like, including from their own
procedure:

```ts
help: t.procedure.query(() => deepHelp(cli.buildProgram()))
```

## Guesses and Assumptions

- The safest first surface is a library helper rather than a built-in command.
  Users can decide whether they want `help`, `deep-help`, or some custom
  procedure name.
- The output should be text, not JSON, because `toJSON()` already exists for
  structured inspection.
- The text should preserve Commander-generated option/argument help for each
  command while adding `=== full command path ===` headings around each block.
- The helper should work with any Commander `Command`, but tests should exercise
  a program produced by `createCli`.
- The implementation should not mutate the program or change normal `--help`
  behavior unless the user wires it in.
- [guess: this is most useful to coding agents because it preserves both routing
  context and exact command usage.]
- [guess: if users like it, a `TrpcCli` method can be added later without
  breaking compatibility.]
- [guess: a single stable format is more valuable than configurability for this
  experimental helper.]

## Checklist

- [x] Run a short grill-you interview to clarify the surface and output shape.
  _Captured decisions in `tasks/complete/2026-05-05-deep-help.interview.md`:
  standalone helper, full help blocks for every visible node, `=== path ===`
  headings._
- [x] Update this task with the resulting decisions and assumptions. _Task now
  records the selected API, output shape, and carried-forward guesses from the
  interview._
- [x] Add tests for recursive help across at least three nested command levels.
  _Added `test/deep-help.test.ts` with a nested `ops admin users` router,
  depth-first heading assertions, full help snapshot coverage, repeated-call
  checks, and hidden Commander command filtering._
- [x] Implement the selected public helper or command surface. _Added standalone
  `deepHelp(program)` in `src/json.ts`, widened `CommanderProgramLike` enough to
  traverse commands, and re-exported the helper from `src/index.ts`; no
  `TrpcCli` method was added._
- [x] Document how users can expose the helper from their own router. _Added the
  README `deepHelp(program)` section with `buildProgram()` usage and a procedure
  exposure example._
- [x] Run focused help tests plus compile or the full suite if shared types
  change. _Ran `pnpm exec vitest run test/help.test.ts test/deep-help.test.ts`,
  `pnpm exec vitest run test/json.test.ts test/deep-help.test.ts`,
  `pnpm compile`, and `pnpm lint`._

## Out of Scope

- Do not replace Commander help globally.
- Do not add markdown, rich terminal formatting, or JSON output in this pass.
- Do not require users to reserve a command name unless the grill-you pass makes
  that tradeoff explicit.

## Implementation Notes

- 2026-05-05: Kept the helper read-only by only reading `name()`,
  `helpInformation()`, `commands`, and Commander hidden markers.
- 2026-05-05: Used Commander's own `helpInformation()` output for every block so
  option, argument, and wrapping behavior stay aligned with normal `--help`.
