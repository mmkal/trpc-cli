---
status: implemented
size: medium
branch: json-mode
---

# Add opt-in global JSON input mode

## Status Summary

Implementation is complete and ready for review. `createCli({jsonInput: true})`
adds `--json <json>` as a complete-input path on procedure commands, while the
default command shape and existing `meta.jsonInput` `--input` behavior remain
covered. Focused JSON/input tests, compile, and lint are passing; the full suite
has not been run.

## Summary Ask

Provide a way to allow:

```sh
mycli some command --json '{"foo":"bar"}'
```

as an alternate input path for every procedure, while preserving the existing
schema-to-flags behavior by default.

The repo already supports per-procedure `meta.jsonInput`, which maps an input
schema to a single JSON option. This task is about an opt-in global surface that
agents and power users can enable for all procedures.

## Guesses and Assumptions

- The feature should be opt-in at `createCli` or `run` time. It should not add a
  global `--json` flag to every CLI by default.
- `--json` should mean "treat this value as the complete procedure input", not
  "format output as JSON".
- The flag name should avoid conflicting with schema-derived options if possible.
  If a procedure already has a `json` option, the implementation should either
  fail clearly or choose a documented escape hatch.
- The implementation should reuse the same parsing and validation path as normal
  command input after producing the POJO input.
- A "global meta defaults" style surface is acceptable if it fits the current
  parser shape better than a separate run option.

## Checklist

- [x] Add tests showing `--json` works for object, primitive, tuple, and nested
  command inputs when the feature is enabled. *Covered in
  `test/json-input.test.ts`, including nested procedure routing and default
  command forwarding.*
- [x] Add a test proving normal CLIs do not get `--json` unless enabled. *The
  opt-in test checks help output and unknown-option behavior without
  `createCli({jsonInput: true})`.*
- [x] Define the public opt-in surface in `TrpcCliParams` or `TrpcCliRunParams`.
  *Added `jsonInput?: boolean` to `TrpcCliParams` and a type assertion in
  `test/types.test.ts`.*
- [x] Implement the global JSON input path with clear conflict behavior. *Leaf
  commands add an internal-keyed `--json <json>` option; it fails clearly when a
  procedure already owns that flag and conflicts with schema-derived options
  when used. A follow-up review pass also made `--json` reject positional
  arguments instead of silently ignoring them.*
- [x] Document the agent-friendly usage pattern and the difference from output
  JSON. *README now shows `createCli({router, jsonInput: true})`, object and
  primitive samples, and states this is input-only.*
- [x] Run focused JSON/input tests plus compile. *Ran
  `pnpm test test/json-input.test.ts test/basics.test.ts test/types.test.ts`
  and `pnpm compile`; also ran `pnpm lint`.*

## Out of Scope

- Do not change result logging or add output serialization modes.
- Do not make `meta.jsonInput` obsolete; it should continue to work.
- Do not infer global JSON mode automatically from `process.argv` inside the
  library.

## Implementation Notes

- Added a `GlobalJsonInputOption` whose Commander attribute name is internal, so
  procedure inputs can still have ordinary `json` properties when global JSON
  input is disabled.
- Commander validates mandatory options and required positional arguments before
  actions run, so the custom `Command` subclass skips those two checks only when
  the internal `--json` option has been supplied.
- `--json` conflicts with schema-derived options through Commander and now also
  rejects supplied positional arguments in the action handler, since its value is
  intended to be the complete procedure input.
- Existing per-procedure `meta.jsonInput` still exposes `--input [json]`; with
  global JSON input enabled, `--json <json>` is also available for the same
  complete-input value.
