name: deps-sync
on:
  push:
    branches: [main, deps]
  pull_request:
    types: [edited]
permissions:
  contents: write
  actions: write
  pull-requests: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: mmkal/runovate@3531bfee4b469ab2ee99b38771bdd2176bdedbd5
