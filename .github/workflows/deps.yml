name: deps-sync
on:
  push:
    branches: [main, deps]
  pull_request:
    types: [edited]
permissions:
  contents: write
  actions: write
  pull-requests: write

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: mmkal/runovate@5fbc80623706316795f4b1e70656b35ac7547284
